using System;

namespace FiguresLibrary
{
	public static class Classificator
	{

		//public static double Triangle(double a, double b, double c)
		//{
		//	if (a < 0 || b < 0 || c < 0)
		//		return 0;

		//	double p = (a + b + c) / 2;

		//	double sumArea = Math.Sqrt(p * (p - a) * (p - b) * (p - c));

		//	return sumArea;
		//}

		//public static double Cirlcle(double r)
		//{
		//	if (r < 0)
		//		return 0;
		//	double sumArea = Math.PI * Math.Pow(r, 2);

		//	return sumArea;
		//}
	}
}
